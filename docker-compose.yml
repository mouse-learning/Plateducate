version: "3"
services:
  flask_server:
    container_name: flask_server
    restart: always
    build:
      context: ./flask_server
      dockerfile: Dockerfile
    environment:
      - FLASK_ENV=dev
      - FLASK_APP=app.py
      - FLASK_RUN_HOST=0.0.0.0
    ports:
      - 5000:5000
    volumes:
      - .:/flask_server
    depends_on:
      - tensorflow-serving
    networks:
      ml-network:
        aliases:
          - flask_server

  tensorflow-serving:
    image: docker.io/bitnami/tensorflow-serving:latest
    container_name: tensorflow-serving
    ports:
      - 8500:8500
      - 8501:8501
    volumes:
      - tensorflow-serving_data:/bitnami
      - ./serving/conf:/bitnami/tensorflow-serving/conf/
      - ./serving/pets:/bitnami/model-data
    networks:
      - ml-network

volumes:
  ml-db:
    name: ml-db
  tensorflow-serving_data:
    driver: local

networks:
  ml-network: